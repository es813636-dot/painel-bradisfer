# Config e skills de agentes de IA (Claude Code, Codex CLI etc.) — locais
# à máquina de cada dev, não fazem parte do painel em si.
.agents/
.claude/
.codex/
.impeccable/
skills-lock.json

# Amostras de resposta da API da Sysemp geradas por
# automacao-vendas/testar-apis-novas.js — contêm dado real de pedido/venda,
# não devem ir pro repositório público.
amostra_*.json
