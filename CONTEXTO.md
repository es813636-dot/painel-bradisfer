# Contexto do projeto — Painel de Estoque Bradisfer

## Links
- **Repositório:** https://github.com/es813636-dot/painel-bradisfer
- **Painel ao vivo:** https://es813636-dot.github.io/painel-bradisfer/
- **Fonte de dados:** Google Sheets (planilha `BaseLooker` + `AnaliseMinMax`), publicado como CSV, ID `1KThPNCmslfoK3zpzxhK6Jh8taj5tKEiNkmsbHTWnV-A`
- **Proposta de direções BI (Power BI):** https://claude.ai/code/artifact/b7276579-517f-48d5-a61f-e5833ee35588

## Como continuar em outro computador
1. Instalar [Git](https://git-scm.com/) e (opcional) [VS Code](https://code.visualstudio.com/).
2. `git clone https://github.com/es813636-dot/painel-bradisfer.git`
3. Isso já traz os 3 arquivos do projeto — `index.html` (esqueleto HTML), `style.css` e `script.js` — tudo em HTML/CSS/JS puro (nenhuma instalação de dependência necessária, sem build).
4. Pra testar localmente com dados reais, **não pode ser aberto direto como arquivo** (`file://`) — o navegador bloqueia o `fetch` pro Google Sheets nesse modo. Suba um servidor estático simples na pasta do projeto (ex.: `python -m http.server 8080`) e acesse via `http://localhost:8080/index.html`.
5. Fluxo de trabalho: editar → testar localmente → `node --check script.js` (checa sintaxe) → `git add`, `git commit`, `git push` → GitHub Pages publica sozinho em ~1 min. **Nada é publicado automaticamente** — só um `git push` explícito manda pro ar.

## O que já foi construído
- Painel de estoque em tempo real (Sysemp → Google Sheets → HTML), com KPIs, gráficos, busca por grupo/marca/produto/situação, rotina de compras semanal, marcas com compra urgente, geração de pedido (CSV pro Sysemp), importação de pedidos em aberto, detalhe de produto com dados ao vivo da Sysemp (via Apps Script).
- Aba "Vendas" com visão estilo BI: produtos/marcas mais vendidos, relatório por marca, e uma seção de "visão geral" com cartões de gradiente — **atenção:** meta, vendedor, ticket médio e evolução por trimestre nessa seção são **dados fictícios**, claramente sinalizados na tela, esperando a API de vendas por vendedor da Sysemp.
- Acessibilidade: navegação por teclado, escape de HTML nos dados da planilha (proteção contra dado malicioso/quebrado), trava de foco em modais.

## Redesign visual (dark elegante + glassmorphism)
Reformulação completa do visual sem mexer na lógica de dados, feita em fases:
- **Design tokens**: nova camada de vars CSS (`--glass-blur`, `--shadow-elevation-1/2/3`, `--radius-sm/md/lg`) somada aos tokens originais (nunca substituídos).
- **Ícones**: todos os ~29 emojis trocados por SVG inline (`SVG_ICONS` + função `icon()` no JS) — sem dependência externa, sem CDN de ícones.
- **Menu de navegação**: sidebar lateral com efeito 3D "porta abrindo" (rotação real via CSS) no desktop; em mobile (≤700px) vira um fade simples — o efeito 3D + `will-change:transform` do wrapper `.scene-3d` causava bugs sérios de rotação/flash no Safari iOS, resolvido restringindo o efeito 3D só a telas largas.
- **Painéis/gráficos**: tratamento glass (blur, sombra, borda) uniforme em todos os cards.
- **"Valor em estoque por marca"**: deixou de ser gráfico Chart.js — agora é uma lista HTML/CSS de barras de progresso (`montarRankingMarcas()`), com paleta categórica por marca (`PALETA_MARCAS`/`corMarca()`) e cores/posição sempre calculadas pela posição **real** no ranking completo (`todasMarcasOrdenadas`), nunca pelo índice local de uma lista já filtrada — evita bug de "toda marca vira #1 dourado" quando filtrada.
- **Modal "Detalhe da marca"**: ao clicar numa marca da lista, abre um modal estático (mesmo padrão do modal de produto — fora de `.scene-3d`/`#app`, nunca `position:fixed` dentro da árvore com `will-change`) com medalha de posição, barra que "tomba" fisicamente (rotação real 0°→90°, não interpolação de forma) e resumo por situação.
- **Modal "Itens mais vendidos"**: abre lado a lado com o de detalhe (mobile: empilhado, ≤860px) mostrando os 10 produtos mais vendidos da marca por Média Mensal — **não mostra "lucro"**, porque o painel só tem custo de compra, não preço de venda; mostrar lucro seria inventar dado. Anima "emergindo" do primeiro modal, e as barras internas só começam a crescer depois que essa animação de entrada termina.
- **Donut "SKUs por situação"**: legenda com mini barra de proporção, animação de entrada com fatias crescendo em cascata, seta customizada aparecendo no hover (linha saindo da borda do anel até um rótulo com nome+valor) — trocou o tooltip nativo do Chart.js.

## Redesign visual — o que falta do plano original
O plano de redesign (dark elegante + reorganização de layout) tinha 5 fases. As 3 primeiras estão prontas (ver seção acima); faltam:
- **Fase 4 — Reconciliar a aba Vendas**: ela usa um tema claro à parte (fundo branco, cores diferentes), com os valores de cor ainda em hex fixo espalhado pelo CSS, não em tokens `--vendas-*`. Falta extrair isso pra tokens (mesmo padrão do resto do painel) e aplicar a mesma escala de sombra/elevação/blur usada nos cards da aba Estoque, pra ficar visualmente consistente entre as duas abas.
- **Fase 5 — Responsividade e regressão final**: reteste dos 3 breakpoints (700px, 860px, mobile pequeno), checagem de `prefers-reduced-motion` em tudo que foi adicionado depois da Fase 3 (ranking de marcas, os dois modais novos, donut com seta de hover), e um passe geral clicando em cada interação do painel pra confirmar que nada quebrou com o acúmulo de mudanças.

## Pendências conhecidas
- **Referência: estrutura das planilhas Excel de gestão de vendas hoje em uso** (analisadas 19/08/2026, arquivos não fazem parte do repositório — `GESTAO VENDAS BRADISFER 2026 2º Semestre.csv` e `FECHAMENTO ALEXANDRE 2026.csv`, enviados pelo usuário como referência). Documentando aqui pra guiar o design futuro do Dashboard de Vendas (aba "Vendas", seção fictícia hoje):
  - **Meta da equipe** dividida em % fixo por marca, em 3 grupos: 5 marcas principais, 5 secundárias, 10 de crescimento.
  - **Meta por vendedor** (9 vendedores) com % de participação no total da equipe.
  - **Faturado vs. meta por vendedor**, com % de aproveitamento.
  - **Detalhe por vendedor**: meta/faturado/% atingido por marca, Ticket Médio, Nº de vendas, média de aproveitamento por grupo de marca.
  - **Campanha de incentivo**: prêmio em R$ por marca se bater 100% da meta — configurado manualmente, não vem da Sysemp (regra interna, não é dado de venda).
  - **Acompanhamento semanal** (meta acumulada crescente vs. atingido) por vendedor, com **feedback qualitativo do gestor** (texto livre) e checklist de temas de avaliação (visitas vs. fechamento, base de clientes, aproveitamento de campanhas etc.).
  - **Problemas de dados encontrados no processo atual** (Excel manual): coluna "Atingido" sem atualizar entre semanas (valor repetido), fórmulas `#DIV/0!` sem proteção quando meta=0, nome de vendedor mal preenchido ("FECHADO"), rótulo de mês desatualizado ("META GERAL NOVEMBRO" em dado de agosto), comentário do gestor citando marcas que não existem em nenhuma outra parte da planilha (provável copia-colou de outro vendedor sem atualizar) — todos sintomas de processo manual sem validação, que um dashboard automatizado eliminaria.
  - O pedido de API enviado à Sysemp (`listarFaturamentoPorVendedor` — vendedor + marca + período + meta + hierarquia supervisor) já cobre o essencial pra recriar isso; só a parte de campanha/prêmio ficaria como configuração manual no painel.
- **Revisar a conversão de Lead Time → meses** em `calcularSugestaoSemPlanilha` (usa `/30`, confirmado correto com prints reais do Sysemp — mês fixo de 30 dias, não 365/12). Já está certo, só ficando registrado que foi validado.
- **Pedir à Sysemp o endpoint de vendas por vendedor/meta** — assim que existir, trocar os dados fictícios da aba Vendas pelos reais (a estrutura visual já está pronta pra isso).
- **Preço de venda real (2026-08-18)** — importado de um export do Sysemp ("manutenção da tabela de preços", CSV com 4.462 produtos, tem margem líquida e preço) e embutido como `TABELA_PRECOS` no `index.html`, casada por `codigoBarras` (~97% dos produtos batem). Usado hoje só no popup lateral (modal de itens mais vendidos de uma marca): cada item mostra que % do valor total vendido da marca ele representa (preço real × média mensal, dividido pelo total da marca) — não é margem/lucro, é participação no faturamento. `margemLucro` também é guardado por produto (vem do mesmo import) mas não está exibido em lugar nenhum ainda. **É uma foto estática do dia da exportação** — preço/margem mudam com reajuste e isso não atualiza sozinho; pra manter vivo, o ideal é subir numa aba nova da mesma planilha Google (mesmo padrão de `BaseLooker`/`AnaliseMinMax`) em vez de reexportar/colar no código toda vez. Ainda não fiz essa migração.
- **Sem lucro/margem exibido em lugar nenhum do painel hoje** — fora o preço de venda acima (usado só pra % de participação no faturamento), o resto do painel só tem custo de compra (`custo`). Onde já existe uma estimativa de faturamento (aba Vendas), continua sinalizada como estimativa, calculada com custo × média vendida.
- **Login Google restrito por e-mail — Nível A implementado (25/08/2026)**. Discussão de governança de dados levou a revisitar o item acima (painel público sem login). Dois níveis de proteção considerados: **Nível A** (login só na tela, sem mudar hospedagem) e **Nível B** (mover hospedagem pro Apps Script + restringir de verdade o compartilhamento da planilha). Usuário optou por começar pelo Nível A.
  - Implementado: `index.html`/`style.css` ganharam um overlay de login (`#login-overlay`) que cobre a tela inteira até autenticar; `script.js` usa **Google Identity Services** (`accounts.google.com/gsi/client`) com um Client ID OAuth do projeto GCP `bradisfer-automacao-vendas` (Origens JavaScript autorizadas: `https://es813636-dot.github.io`). Decodifica o JWT devolvido só pra ler o e-mail (sem validar assinatura — não dá pra validar de verdade sem backend) e compara contra `EMAILS_PERMITIDOS` (hoje: `marcusmatos19@gmail.com`, `bradisferdistribuuidora@gmail.com`). Login válido fica salvo no `localStorage` por 12h.
  - **Limitação conhecida, documentada de propósito**: isso protege só a *tela* do painel. A URL do CSV público da planilha (gviz, usada em `CSV_URL`/`ANALISE_CSV_URL`/`VENDAS_VIVO_CSV_URL`) continua acessível direto pra quem souber/achar o link — o Client ID OAuth em si não é segredo (protegido pela lista de origens autorizadas, não por sigilo), mas o CSV da planilha em si segue com compartilhamento "qualquer pessoa com o link". Proteção de verdade (esconder os dados também, não só a tela) exigiria o **Nível B** — mover a hospedagem do painel pro Apps Script, servir os dados só depois de validar login server-side, e aí sim restringir o compartilhamento da planilha. Não implementado ainda; decisão de ficar só no Nível A por ora foi consciente do usuário.
  - Pra adicionar/remover alguém com acesso: editar `EMAILS_PERMITIDOS` em `script.js`.
- **Escolher uma das 4 direções de Power BI** propostas (link acima) se quiser seguir esse caminho — requer Power BI Desktop, que eu não consigo operar remotamente.
- **Migração das vendas do painel pra "AO VIVO" (Sysemp) em vez da planilha `AnaliseMinMax` congelada** — concluída (19/08/2026). Bloco de funções no Apps Script (`atualizarVendasAoVivo`, `buscarVendaMediaUmProduto`, `criarGatilhoVendasAoVivo`) roda em lote a cada **10 min** (~5 min/rodada, ~450 produtos/rodada — mesmo em conta Workspace, o teto real de execução aqui é 6 min, não 30; provavelmente porque quem executa não é a conta dona do domínio), gravando Média Mensal/Total Vendido de cada produto (`listarVendasMediaPorProduto`, últimos 12 meses) numa aba `VendasAoVivo`, retomando de onde parou. Primeiro ciclo completo: **4.517 produtos, 0 erros**. `index.html` busca essa aba junto com `BaseLooker`/`AnaliseMinMax` (campo `vendasAoVivoLote` por produto) e passou a ser a fonte principal do cálculo de sugestão de compra em 2 lugares: `gerarPedidoSysemp` (geração do CSV de pedido) e o modal de detalhe do produto (card "Sugestão de Compra") — a planilha `AnaliseMinMax` virou só fallback (produto ainda não coberto pelo lote) e contexto informativo (Curva ABC, nível de atendimento continuam exibidos, mas não alimentam mais o número). Motivação: a base histórica da `AnaliseMinMax` foi um import único, nunca mais atualizado — divergência confirmada em itens de giro alto (ex. caixa organizadora INJEPLASTEC: AnaliseMinMax dizia 10 un/mês, AO VIVO real é ~6.600 un/mês).
- **⚠️ Bug conhecido na API de vendas da Sysemp: conta transferência entre empresas como venda** (identificado 19/08/2026). A Bradisfer tem 3 empresas cadastradas na Sysemp; toda compra é feita por 1 empresa e depois transferida pras outras 2 via CFOP 5102 com ID de operação 39 — venda real (cliente final) usa o mesmo CFOP 5102, mas ID de operação 1. A API `listarVendasMediaPorProduto` está somando as duas juntas, inflando "Média Mensal"/"Total Vendido" pra qualquer produto que circula entre as empresas (confirmado com o exemplo da HIPERFITA: pedido gerado com valores ~2x maiores que o esperado). Usuário já pediu à Sysemp pra excluir CFOP 5102 ID 39 do cálculo — assim que aplicarem, os números da `VendasAoVivo` corrigem sozinhos, sem precisar mexer no código daqui. **Decisão (19/08/2026): deixar a migração AO VIVO rodando mesmo assim**, sabendo que alguns itens podem estar com pedido sugerido superestimado até a Sysemp confirmar o filtro — não foi revertido pra `AnaliseMinMax`. **Reteste em 20/08/2026 ficou inconclusivo**: comparamos o produto CESTO LIXO 100L PLASTICO (`7897649768629`) entre a API (oscilando 43k-47k de total/12M em consultas diferentes, mesma janela) e a tela "Histórico do Produto" do próprio Sysemp — com o filtro "Somente com movimentação de Estoque" e período "até" em branco, a tela mostrou só 22.315 (parecia confirmar o bug); mas com o período completo (19/08/2025 a 19/08/2026) e "Todas as movimentações", a tela mostrou 46.860 — quase idêntico ao valor da API. Ou seja, o primeiro print provavelmente era um resultado parcial (período incompleto), não prova de bug. Segue sem confirmação definitiva se a correção do CFOP está ou não valendo.

- **Cota diária do Google Apps Script estourada (20/08/2026)** — o lote `atualizarVendasAoVivo` rodando a cada 10 min (~450 chamadas/rodada) somado ao `atualizarEstoque` e testes manuais excedeu a cota de **100.000 chamadas HTTP/dia** (confirmado official: contas Workspace têm 100k/dia, contas pessoais 20k/dia — não há diferença por plano Business Starter/Standard/Plus). Erro característico: `Exception: Service invoked too many times for one day: urlfetch`. Investigamos migrar pra outra conta Google, mas a tela "Detalhes do Projeto" (ícone "ⓘ" no Apps Script) mostrou que a conta atual já é a dona (Proprietário: Eu) e já é Workspace — migrar não ajudaria. **Mitigações aplicadas:**
  - Gatilho do `atualizarVendasAoVivo` mudado de 10 para **30 minutos** (~48 rodadas/dia em vez de ~144) — reduz o consumo diário de ~65 mil pra ~22 mil chamadas só dessa função.
  - Adicionado guard de madrugada (pula execução entre 0h-4h59) em `atualizarVendasAoVivo`, igual ao que `atualizarEstoque` já tinha.
  - **Bug corrigido:** quando uma tentativa de atualizar um produto dava erro (ex. cota estourada), o código sobrescrevia a linha da planilha `VendasAoVivo` com uma linha vazia, apagando o último dado bom que já existia de um ciclo anterior. Corrigido pra só gravar linha de erro quando o produto **nunca** tinha sido processado com sucesso — se já tinha dado bom, mantém ele até conseguir atualizar de verdade.
  - **Pedido enviado à Sysemp (20/08/2026):** endpoint de consulta de vendas em lote (`pedido-api-vendas-em-lote.pdf`) — pedindo pra `listarVendasMediaPorProduto` aceitar lista de códigos de barras numa chamada só, ou paginação por `offset` sem filtro de código (mesmo padrão do endpoint de estoque). Reduziria o ciclo completo de ~4.500 chamadas pra ~45.
  - **A Sysemp implementou no mesmo dia** (20/08/2026) — sem `cod_barra` no corpo da chamada, `listarVendasMediaPorProduto` devolve o catálogo inteiro paginado por `offset` (100 por página, ~46 chamadas cobrem tudo). **Migração completa pro GitHub Actions** como resultado: `atualizarVendasAoVivo` e `atualizarEstoque` saíram do Apps Script e viraram workflows (`automacao-vendas/atualizar-vendas.js` + `.github/workflows/atualizar-vendas.yml`, hora em hora; `automacao-vendas/atualizar-estoque.js` + `.github/workflows/atualizar-estoque.yml`, a cada 10 min) — sem depender de cota nenhuma do Google (GitHub Actions em repositório público não tem limite de minutos). Autenticam na planilha via conta de serviço do Google (`vendas-ao-vivo-github@bradisfer-automacao-vendas.iam.gserviceaccount.com`, projeto GCP `bradisfer-automacao-vendas`, compartilhada como Editor na planilha). Os gatilhos antigos do Apps Script pra essas 2 funções foram removidos. **Decisão de arquitetura final:** o `doGet` (clique individual num produto no painel, resposta síncrona pro usuário) **continua no Apps Script** — GitHub Actions não serve pra responder um clique em tempo real (workflow demora de segundos a minutos pra subir/rodar, não dá resposta instantânea pro navegador). Os dois se completam: lote grande (catálogo inteiro) no GitHub Actions, clique individual (baixo volume, precisa ser rápido) no Apps Script.
  - Achado nesse processo: pegar um item específico (código de barras) via `doGet` às vezes demora ~20s+ sem resposta — não é bug nosso, é sobrecarga própria de infraestrutura do Apps Script (redirecionamento interno do Google pra `script.googleusercontent.com`), já documentada no código com timeout de 30s + fallback.
- **Janela de tempo pra sugestão de compra: decidido manter 12 meses, não trocar por janela curta** (19/08/2026). A Sysemp tem uma tela nativa "Sugestão Customizada de Compras" (Dias Atrás/Dias Futuros configurável) que usa uma janela recente (ex. 30 dias) em vez de média anual — testamos pra caixa organizadora INJEPLASTEC: 12 meses = 6.608/mês, 90 dias = 13.044/mês, 30 dias = 15.186/mês (tendência real de alta, não pico isolado). Cogitamos guardar as duas janelas na `VendasAoVivo`, mas o usuário decidiu manter só 12 meses — a lógica atual (média longa + lead time real por marca, que a tela da Sysemp não usa) foi considerada melhor pra sugestão de compra. Também mandamos pergunta pra Sysemp perguntando se existe API pra essa tela "Sugestão Customizada de Compras" (Curva ABC, Cobertura de Estoque, Sugestão de Compra prontos) — se vierem a liberar, substituiria o cálculo próprio; resposta ainda pendente.
- **Dividir o `index.html` em arquivos separados** — concluído (19/08/2026, execução autônoma). `index.html` (esqueleto HTML puro, ~65 linhas) + `style.css` (982 linhas) + `script.js` (2.736 linhas, inclui `TABELA_PRECOS` — não foi extraída num JSON separado, ficou pra depois se algum dia valer a pena). Extração mecânica via script Node (linha a linha, com checagem de que as tags `<style>`/`<script>` batiam exatamente antes de cortar) + backup do arquivo original antes de mexer. Testado localmente antes do push: sintaxe OK, cor de fundo do CSS aplicada, Chart.js carregando, `dadosCompletos` populado (4.137 produtos), modal de produto abrindo com dado AO VIVO — tudo idêntico ao comportamento de antes. Continua "zero build step": só `<link rel="stylesheet">` e `<script src="">` estáticos, mesmo fluxo de `git push`.
- **Código de barras perdendo zero à esquerda no Google Sheets (25/08/2026)** — usuário reportou que o Sysemp não reconhecia o pedido importado da marca COLOR&COLA (item cadastrado como `0074468051034`, 13 dígitos EAN-13). Investigação longa, com becos sem saída registrados aqui pra não repetir:
  1. Primeira hipótese (parcialmente certa, mas não a causa principal): a Sysemp podia devolver `cod_barra` como número no JSON, perdendo zeros à esquerda. `normalizarCodigoBarras()` (completa pra 13 dígitos) ficou como rede de segurança, mas sozinha não resolveu — o valor bruto da API já vinha certo na maioria das vezes.
  2. Becos sem saída (nenhum resolveu sozinho): travar o formato da coluna como "Texto simples" via `batchUpdate`/`repeatCell`; escrever com `USER_ENTERED` + aspa simples como *dica* de formatação; gravar como fórmula de string literal (`="0074468051034"`). Em todas essas tentativas, o valor aparecia certo logo após a escrita (inclusive confirmado lendo de volta direto pela API), mas voltava a aparecer sem os zeros minutos depois — sem nenhuma automação nossa rodando no meio, e com os Gatilhos do Apps Script e `onEdit`/`onOpen` simples confirmados como inexistentes (não é gatilho nenhum causando isso).
  3. **Fix real**: uma aspa simples como parte do **conteúdo literal** da célula (ex. `'0074468051034`, com a aspa fazendo parte da string, não como dica de digitação da UI). Diferente das tentativas anteriores, esse valor nunca é só-dígitos, então fica estruturalmente impossível o Sheets reinterpretar como número em qualquer recálculo — confirmado estável no Produtos e propagando certo até a BaseLooker.
  - `normalizarCodigoBarras()` em `atualizar-estoque.js`/`atualizar-vendas.js` agora sempre prefixa com `'`. `script.js` ganhou `limparCodigoBarras()`, que tira essa aspa de volta nos 2 pontos que leem Código Barras da planilha (BaseLooker e VendasAoVivo) — o resto do painel nunca vê a aspa.
  - **Cuidado ao mexer nisso de novo**: em `atualizar-vendas.js`, a chave de comparação usada pra casar/atualizar linhas existentes (`mapaLinhas`) precisa sempre tirar a aspa antes de comparar — sem isso, qualquer mudança futura no prefixo duplicaria a aba inteira numa única rodada de transição (linha antiga com chave diferente da nova nunca é removida).
  - **Lição de diagnóstico pra próxima vez que algo parecer "voltar a dar errado sozinho" numa planilha**: o CSV público (`gviz`) tem cache real e imprevisível — não serve pra confirmar nada com certeza, mesmo com cache-buster na URL. A única forma confiável de checar o estado real é o usuário olhando a célula ao vivo na UI do Sheets (F5 + conferir a barra de fórmulas), ou lendo via `values.get` direto pela API dentro do próprio script (não pelo CSV).
- **Busca de vendas em paralelo — reduz o ciclo de ~10min pra ~3min (25/08/2026)**. `atualizar-vendas.js` buscava as ~46 páginas do catálogo (100 produtos cada) uma de cada vez, sequencial (~450-660s por ciclo). A Sysemp confirmou que não trava por tempo do lado deles, então trocamos por busca em "ondas" paralelas via `Promise.all` (`PAGINAS_EM_PARALELO`): busca a 1ª página sozinha pra descobrir o tamanho de página do ciclo, depois dispara N páginas simultâneas por onda, parando quando alguma vier vazia/parcial (fim do catálogo). Testado isoladamente com mock antes de subir (catálogo real, múltiplo exato de página, menos de 1 página, vazio, 1 item só — todos ok, sem duplicata).
  - **Resultado dos testes de paralelismo** (rodadas reais, via workflow_dispatch): 4 → 233s/251s; 6 → 193s; 8 → 225s/171s/203s (média ~200s, estável em 3 rodadas); **10 → 504 Gateway Time-out no offset 3600** (falhou depois de 36 páginas ok). Ficou em **8** — maior valor confirmado estável. Uma falha nessa busca não corrompe a planilha (a exceção interrompe `buscarTodasVendas` antes de `main()` chegar em qualquer escrita no Sheets), só perde o ciclo.
  - Não subir esse número sem testar de novo com cuidado — está documentado como comentário em `atualizar-vendas.js` também.
  - **Mesmo padrão aplicado no `atualizar-estoque.js`** (mesmo dia): esse endpoint já era rápido sozinho (~45-70s), mas por consistência entre os dois scripts ganhou o mesmo `PAGINAS_EM_PARALELO=8`. Testado: **24s** (era 45-50s), 4.527 produtos confirmados na planilha, sem erro. Diferença de implementação: aqui o tamanho de página é fixo e conhecido (`LIMITE_POR_PAGINA=100`), não precisa da etapa de descobrir dinamicamente como no vendas.
- **Migração do clique individual (modal de produto) do Apps Script pro Cloudflare Worker** (25/08/2026). Motivação: usuário queria reduzir o tempo de abertura do modal ao clicar num produto; o `doGet` do Apps Script tinha overhead de infraestrutura própria do Google (~20s+ às vezes, redirecionamento interno pra `script.googleusercontent.com`), já documentado antes. Conversa também trouxe o tema de **governança de dados** — criado `LINHAGEM-DE-DADOS.md` documentando de onde vem cada dado, quem atualiza, e a validade de cada fonte (motivado pela investigação do código de barras, onde a aba `Base` intermediária só foi descoberta no meio da depuração).
  - Usuário colou o código completo do Apps Script (projeto "Estoque") pra eu replicar exatamente o comportamento do `doGet` — mesmos endpoints (`listarComprasPorProduto`, `listarVendasMediaPorProduto`), mesmo payload, mesma janela de datas (hoje - 1 ano até hoje, formato DD/MM/AAAA em America/Sao_Paulo).
  - Criado `cloudflare-worker/produto-detalhe.js`, testado isoladamente (mock de fetch: parâmetro faltando, token faltando, payload de compras, payload de vendas com datas corretas, preflight CORS) antes do deploy.
  - Deploy manual guiado passo a passo (usuário sem experiência prévia com Cloudflare): criar conta → Workers → "Comece com Olá, mundo" → colar código em "Editar código" → `SYSEMP_TOKEN` como Secret em Settings → Variables and Secrets. URL final: `rough-dust-49b2.bradisferdistribuuidora.workers.dev`.
  - **Resultado medido em produção, comparado ao Apps Script antigo**: "compras" caiu de 2,9s pra 0,36s (~8x mais rápido — confirma que era mesmo overhead do Apps Script). "Vendas" ficou parecido (Apps Script 10,9s; Worker variando 5,5s-15s, uma vez 52,7s) — **achado importante**: o gargalo de "vendas" não é infraestrutura de transporte, é a própria Sysemp demorando pra calcular 1 ano de histórico de venda pra 1 produto só. Trocar de servidor não resolve isso; só ajustar a consulta (janela menor) ou a Sysemp otimizar do lado deles resolveria.
  - `WEBAPP_URL` em `script.js` trocado pro Worker. Apps Script "Estoque" fica 100% desativado (nenhuma função mais chamada), mas não foi excluído — ver lacuna conhecida em `LINHAGEM-DE-DADOS.md`.
  - **Fix de verdade (mesmo dia, logo em seguida)**: usuário reportou que o modal continuava lento "por conta das vendas" mesmo depois da troca de servidor — como já esperado (o gargalo era a Sysemp, não o transporte). Isso levou a perceber que a busca de "vendas" ao vivo por clique era **redundante**: os mesmos campos (média mensal, total vendido, última venda) já vêm carregados na memória do navegador desde o início, via `VendasAoVivo` (sincronizada de hora em hora, guardado em `produto.vendasAoVivoLote`). `buscarComprasVendasReais()` agora usa esse dado local direto, sem chamar a Sysemp de novo, e só busca "vendas" ao vivo como fallback pra produtos ainda fora do ciclo de sincronização. "Compras" continua sempre ao vivo (não tem equivalente em lote). Resultado: a chamada mais lenta (5-15s) desaparece na maioria dos cliques — o modal fica limitado só pela busca de compras (~0,36s via Worker).

- **Aba "Atenção" — motor de regras determinístico (31/08/2026)**. Usuário pediu um "bot/IA" que analisasse os dados e apontasse itens/marcas que precisam de atenção, cruzando Curva A, lead time e média de vendas. Perguntado sobre custo de API de LLM, optamos por **não usar IA nenhuma**: os dados já são estruturados e a regra é bem definida, então virou uma função determinística (mesma entrada sempre dá a mesma saída, sem custo por consulta, sem latência de rede). Implementado em `script.js`:
  - `calcularCurvaAoVivo(dados)` — ranqueia o catálogo por faturamento mensal estimado (média de venda × `precoVenda`, este último vindo só da `TABELA_PRECOS` estática) e classifica em A (até 80% do faturamento acumulado), B (até 95%) ou C (resto) — Curva ABC clássica, recalculada a cada carregamento com dado fresco, diferente da curva da planilha `AnaliseMinMax` que está congelada desde a importação.
  - `calcularAlertas(dados)` — cruza as duas curvas (ao vivo e planilha, mostradas lado a lado por pedido do usuário) com cobertura de estoque em dias (`estoque ÷ venda média diária`) e lead time por marca (`LEAD_TIME_POR_MARCA`). Marca **CRÍTICO** quando a cobertura é menor que o lead time (vai faltar antes da reposição chegar) ou já está em ruptura sendo item relevante; marca **ATENÇÃO** quando a folga é menor que 30% ou está abaixo do mínimo sendo curva A. Só considera itens curva A/B — item C não gera alerta aqui.
  - Nova aba no painel (`renderizarAbaAtencao()`, botão "Atenção" na barra de navegação) seguindo o sistema visual escuro padrão (kpi-grid/panel/table), não o tema claro da aba Vendas. Mostra cartões-resumo, ranking de marcas em alerta e tabela de itens (clicável, abre o modal de produto de sempre).
  - Testado localmente com o catálogo real antes do push: 384 críticos + 57 em atenção, R$ 675 mil de faturamento mensal em risco, 78 marcas afetadas — números plausíveis pra conferência inicial pelo usuário.

- **Camada de IA (Claude) por cima do motor de regras, na aba Atenção (31/08/2026)**. Depois de aprovar o motor de regras determinístico (ver entrada acima), o usuário pediu a parte de "bot/IA" que tinha sido adiada por causa do custo — perguntado quanto custaria, optamos por Claude Sonnet 5 (US$ 2/US$ 10 por milhão de tokens entrada/saída) e o usuário escolheu ter os dois formatos: **resumo automático** (ao abrir a aba) e **chat livre** (perguntas soltas).
  - **Decisão de arquitetura**: a IA nunca calcula números — só interpreta e prioriza em texto o que `calcularAlertas()` (motor de regras, já existente) calculou. Evita alucinação de número e mantém o motor de regras como fonte única da verdade.
  - **Custo controlado por design**: `montarContextoIA()` em `script.js` condensa a saída do motor de regras pros 15 marcas + 25 itens mais relevantes antes de mandar pra IA — não o catálogo inteiro. Isso mantém o tamanho (e custo) de cada chamada previsível, independente de quantos produtos o painel tem.
  - **Chave da API nunca chega no navegador**: mesmo padrão do `SYSEMP_TOKEN` — o Cloudflare Worker (`cloudflare-worker/produto-detalhe.js`, o mesmo já usado pra Sysemp) ganhou uma rota nova (`POST`, JSON) que recebe o contexto condensado do navegador, chama a API da Anthropic com `ANTHROPIC_API_KEY` guardada como Secret, e devolve só o texto de resposta. Guiar o usuário a criar a chave em console.anthropic.com e configurar o Secret no Cloudflare foi feito por texto no chat (nunca inserindo a chave por mim — é credencial, ver regras de segurança da sessão).
  - **Chat sem estado no servidor**: como o Worker é stateless, cada chamada de chat reenvia o contexto condensado + o histórico da conversa (`chatIAHistorico`, guardado só no navegador) — o custo cresce com o tamanho da conversa, não com o catálogo.
  - **Nunca dispara chamada à IA num re-render qualquer** — só quando o usuário entra na aba pela primeira vez (resumo, uma vez por sessão até clicar "Atualizar análise") ou envia uma pergunta no chat. Sem isso, qualquer clique/filtro na aba dispararia uma chamada nova, sem necessidade.
  - Testado ao vivo em produção (não só local) depois do usuário configurar o Secret: resumo automático identificou corretamente a INJEPLASTEC como prioridade #1 (R$ 184.839/mês em risco, cobertura de 20 dias contra lead time de 38), e o chat respondeu de forma consistente com o mesmo resumo pra "qual marca eu deveria priorizar hoje?".
- **IA movida pra aba própria no menu principal, sob pedido (31/08/2026)**. Depois de testar em produção, o usuário pediu duas mudanças: (1) acessar a IA pelo menu principal, não escondida dentro da aba Atenção; (2) ela só responder quando o usuário perguntar algo — sem disparo automático ao abrir a aba.
  - Aba **Atenção** voltou a ser só o motor de regras (`renderizarAbaAtencao()`) — kpi-cards, ranking de marcas e tabela de itens, 100% determinístico, sem nenhuma chamada de IA.
  - Nova aba **IA** (`renderizarAbaIA()`, 4º botão na barra de navegação, ícone `robotIcon` novo) reúne o resumo (agora com botão "Gerar resumo" que só dispara no clique — nunca mais ao entrar na aba) e o chat, ambos usando o mesmo `montarContextoIA()` calculado por baixo a partir do motor de regras da aba Atenção.
  - Testado localmente com mock do Worker: abrir a aba IA mostra só um placeholder ("Nenhum resumo gerado ainda"), sem nenhuma chamada de rede disparada sozinha; resumo e chat funcionam sob demanda; trocar de aba e voltar preserva o histórico do chat (estado em memória, não persiste entre recarregamentos de página).
- **IA vira botão flutuante, acessível de qualquer aba (31/08/2026)**. Usuário testou a aba "IA" e pediu pra trocar por um ícone flutuante na tela principal — acesso rápido de qualquer lugar do painel, não escondido dentro do menu lateral.
  - Removida a aba "IA" do menu (4º botão em `barraAbas()`). Adicionado botão flutuante fixo (`#fab-ia-btn`, canto inferior direito, ícone `robotIcon`) e um painel (`#painel-ia-flutuante`) — ambos no HTML estático (`index.html`), **fora de `#app`**, então sobrevivem ao `innerHTML` ser reescrito toda vez que `renderizar()` roda (troca de aba, filtro, atualização de dados).
  - `renderizarPainelIAFlutuante()` (novo, em `script.js`) é a função que desenha o conteúdo do painel (resumo + chat) — chamada só quando o próprio painel muda de estado (abrir/fechar, resumo carregando/pronto, mensagem de chat enviada/respondida), nunca pelo `renderizar()` geral do resto do painel.
  - Continua sem disparo automático: resumo só gera no clique em "Gerar resumo", chat só responde no envio — mesma regra da versão em aba, só mudou o lugar.
  - Testado localmente (mock do Worker): botão abre/fecha o painel, resumo e chat funcionam, e trocar de aba com o painel aberto (ex. clicar em "Vendas") mantém o painel aberto com o resumo e o histórico de chat intactos — confirma que ele realmente não depende de `#app`.

## Convenções do projeto
- **Checar a sintaxe do JS antes de todo push.** Desde a divisão em arquivos (19/08/2026), o JS mora em `script.js`, separado do `index.html` — um erro de sintaxe nele não falha em lugar nenhum, só impede o script inteiro de executar, e a página fica congelada pra sempre no esqueleto inicial ("Buscando dados na planilha..."). Já aconteceu (quando ainda era tudo um arquivo só) de remover um bloco HTML de dentro de um ternário e deixar o ramo `else` faltando — o painel ficou fora do ar e parecia problema de cache/GitHub Pages. Comando pra validar:
  ```
  node --check script.js
  ```
  Depois disso, subir o servidor local e confirmar que a página realmente renderiza — sintaxe válida não garante que nenhuma variável ficou órfã.
- Paleta de status validada contra daltonismo/contraste (skill `dataviz`, script `validate_palette.js`): RUPTURA `#e66767`, BAIXO `#a87c0e` (âmbar — trocado do verde `#199e70` original pra passar mais urgência; testado contra vários tons de laranja/âmbar, a maioria falhava a checagem de separação CVD contra o vermelho de RUPTURA), EXCESSO `#3987e5`, OK `#008300`. Antes de trocar qualquer cor de status, rodar o validador de novo — não escolher visualmente.
- Gráficos Chart.js (donut) têm o `<canvas>` sempre preservado entre renderizações (nunca destruído/recriado à toa) via `replaceWith()` + checagem `instance.canvas.isConnected`, senão a animação não tem "de onde" partir.
- Animação dos gráficos só liga em clique real do usuário (chip/card/legenda/barra), nunca em toda renderização — evita travar em 0 quando a aba está em segundo plano, e evita reanimar em toda tecla digitada numa busca.
- **`style="width:X%"` inline sempre vence sobre qualquer `width` do CSS**, incluindo dentro de `@keyframes` — isso já causou bug de "barra sem animação" duas vezes neste projeto (barras de progresso HTML/CSS). Solução padrão: passar o valor via CSS custom property inline (`style="--pct-barra:X%"`) e deixar o `width` real ser definido inteiramente pelo CSS/keyframe, nunca direto no elemento.
- **`will-change: transform`** (ou qualquer `transform` real) num elemento ancestral cria um novo "containing block" — qualquer `position: fixed` dentro dele passa a ser relativo a esse ancestral, não ao viewport. O wrapper `.scene-3d` (menu 3D) tem isso na base; qualquer novo overlay/modal precisa ficar fora dessa árvore no HTML estático (mesmo padrão do `#modal-backdrop` original), nunca tentar virar `position:fixed` de dentro dela via JS.
- Reaplicar uma classe que carrega uma `animation` CSS **reinicia essa animação do zero**, mesmo com `transition:none` — `transition` e `animation` são mecanismos independentes. Pra restaurar um estado visual final sem repetir a animação (ex. depois de um re-render), usar uma classe irmã sem `animation`, só com o `transform`/propriedade final direto (padrão usado em `.tombado-fixo` como par de `.tombando`).
